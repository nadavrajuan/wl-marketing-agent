# WL Marketing Agent

## BigQuery schema

**Always read `BIGQUERY.md` before writing or reviewing any BigQuery SQL in this project.**
It is the authoritative source of truth for table schemas, column types, and join patterns.
Never assume column types from training data — verify against `BIGQUERY.md` first.
